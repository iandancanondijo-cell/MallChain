# Template only — DO NOT apply as-is or commit real values. Generate the
# real Secret out-of-band (kubectl create secret generic ... --from-literal,
# or a sealed-secrets/external-secrets-operator pipeline synced from a real
# secret manager) and never check the filled-in version into git. This file
# exists so the *shape* the Deployments expect (see 10-backend.yaml's
# envFrom) is documented and reviewable.
apiVersion: v1
kind: Secret
metadata:
  name: mallchain-backend-secrets
  namespace: mallchain
type: Opaque
stringData:
  MONGO_URI: 'mongodb://mallchain:REPLACE@mongo-host:27017/marketplace?authSource=admin'
  JWT_SECRET: 'REPLACE_WITH_RANDOM_32PLUS_CHAR_SECRET'
  SESSION_SECRET: 'REPLACE_WITH_RANDOM_32PLUS_CHAR_SECRET'
  ADMIN_API_KEY: 'REPLACE_WITH_RANDOM_32PLUS_CHAR_SECRET'
  PAYMENT_WEBHOOK_SECRET: 'REPLACE_WITH_PROVIDER_WEBHOOK_SECRET'
  SAFARICOM_KEY: 'REPLACE'
  SAFARICOM_SECRET: 'REPLACE'
  VAULT_ADDR: 'REPLACE'
  VAULT_TOKEN: 'REPLACE'
